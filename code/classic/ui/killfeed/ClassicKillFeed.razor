@namespace SpeedDial.Classic.UI

@attribute [StyleSheet("code/classic/ui/killfeed/ClassicKillFeed.scss")]

<root>
    <!--if we decide to style the killfeed container itself
        like with a header or some shit
        -->
</root>